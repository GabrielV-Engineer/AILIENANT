**Linked issue:** Fixes # / Closes #

**What changed and why**

**How was this verified?**

- Tests added/run:
- `mypy` / `pyright` / `ruff` / `npm run compile` / `npm run lint`: pass?
- Does this touch a `mypy`-strict module or a blueprint-governed contract?

**UI changes** — before/after screenshots or a short clip, if applicable.

---

Checklist:
- [ ] Conventional Commits format for the title and commits (`type(scope): summary`)
- [ ] One concern per PR — kept small and focused
- [ ] No secrets committed
