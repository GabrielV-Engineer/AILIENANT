# ailienant-core/validators/__init__.py
"""Phase 4.2 — Deterministic Validators (non-LLM, zero-token gate nodes).

Modules:
    environment — verify_environment_node (interpreter + typing-config probe)
    gates       — syntax_gate_node, style_gate_node, pure helpers
"""
