{
  "name": "ailienant",
  "version": "0.0.0",
  "private": true,
  "description": "Repo-wide tooling root — not a publishable package. Pins spec-governance CLI dependencies decoupled from ailienant-extension's own packaging.",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "openspec:validate": "openspec validate --all --strict --json --no-interactive"
  },
  "devDependencies": {
    "@fission-ai/openspec": "^1.9.0"
  }
}
